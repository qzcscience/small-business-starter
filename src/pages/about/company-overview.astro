---
import AboutDetailShell from '../../components/about/AboutDetailShell.astro';
import { getAboutSection } from '../../data/about';

const section = getAboutSection('company-overview')!;
---

<AboutDetailShell
  section={section}
  showSummary={false}
  showHighlights={false}
  showBackLink={false}
  showCta={false}
>
  <div class="space-y-4">
    {section.blocks.map((block) => (
      <section aria-label={block.title}>
        <div class="space-y-4">
          {block.body.map((paragraph) => (
            <p class="text-base leading-relaxed text-text-muted">{paragraph}</p>
          ))}
        </div>
      </section>
    ))}
  </div>
</AboutDetailShell>
